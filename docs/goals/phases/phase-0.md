## Phase 0 — Land pending work, clean slate                                                                                                                                                                                         
Goal: green CI on `main` before touching anything new.                                                                                                                                                                              
                                                                                                                                                                                                                                    
1. Branch `chore/phase-0-cleanup`. Stage and commit the existing working-tree changes from the prior session in logical groups:                                                                                                     
   - `feat(lan): expose backend on LAN with bearer-token auth` (network_config.py, auth.py, __main__.py, settings.py route + schema, NetworkSection.tsx, sidebar entry)                                                             
   - `fix(schedule): tz-naive datetime handling + public-default privacy` (schedule.py, scheduled.py, schemas/schedule.py, AutoScheduleDialog.tsx)                                                                                  
   - `fix(tauri): prevent double-open of external URLs` (lib/tauri.ts)                                                                                                                                                              
   - `chore(scripts): add flip_pending_privacy helper`                                                                                                                                                                              
2. Fix the two pre-existing tsc errors so `build:strict` passes:                                                                                                                                                                    
   - `SeriesDetail/_monolith.tsx:550` (state type mismatch)                                                                                                                                                                         
   - `PlatformCard.tsx:14` (unused `socialApi` import — remove or wire up)                                                                                                                                                          
3. Filter virtual adapters in `lan_ipv4_addresses()` — drop `172.16.0.0/12` WSL/Docker ranges, `vEthernet`, Hyper-V switches, and link-local `169.254.0.0/16` so the LAN panel only lists reachable URLs.                           
4. Generate an Alembic migration aligning `schedule.server_default` with the new `public` default — even if harmless today, future-proof it.                                                                                        
5. Live-verify the Tauri double-open fix: build the Windows installer, click a YouTube link in the Calendar drawer, confirm one tab opens (not two). Document the test in `CHANGELOG.md`.                                           
6. If no e2e harness exists, scaffold Playwright against the dev server with one smoke test per top-level route (just `expect(page).toHaveURL` + a screenshot). Land it as `chore(test): scaffold e2e smoke suite`.                 
                                                                                                                                                                                                                                    
Acceptance: `main` is green, installer runs, smoke tests pass.            
