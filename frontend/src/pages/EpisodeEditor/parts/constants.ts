// MIME-ish keys used to move assets / stamps from the right-rail
// into the timeline via HTML drag-and-drop.

/** Carries an asset id (UUID string). */
export const ASSET_DRAG_MIME = 'application/x-drevalis-asset';

/** Carries a stamp id (catalog string). */
export const STAMP_DRAG_MIME = 'application/x-drevalis-stamp';
