"""Desktop CLI subcommands.

Imported lazily by :mod:`drevalis.__main__` so that ``python -m drevalis``
without a subcommand (the default launcher path) doesn't pay the import
cost of httpx / faster-whisper / etc.
"""
